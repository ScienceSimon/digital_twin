const config = {
    MQTT_HOST: window._env_.MQTT_HOST,
    MQTT_PORT: window._env_.MQTT_PORT,
    MQTT_USER: window._env_.MQTT_USER,
    MQTT_PASS: window._env_.MQTT_PASS, 

};

export default config;